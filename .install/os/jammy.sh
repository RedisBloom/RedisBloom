#!/usr/bin/env bash
# Ubuntu 22.04 (jammy). Default Debian install — apt's gcc/g++/cmake on
# 22.04 are recent enough for the build.

# shellcheck source=../lib/sets.sh
. "$LIB/sets.sh"

debian_default_install
