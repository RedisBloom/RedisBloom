DOCKER_ORG=redisfab
PRODUCT=rebloom
REDIS_VERSION=6.2.4
OSNICK?=buster
ROOT=../../
READIES=${ROOT}/deps/readies
DOCKER_TAGS=redislabs/redis:${REDIS_VERSION}-${ARCH}-${OSNICK}

include ${READIES}/mk/docker.rules
